DJANGO_SHELL?=$(shell find **/manage.py) 
DJANGO_SETTINGS?=src.settings
DJANGO_BIN=$(ENV) django-admin --settings=$(DJANGO_SETTINGS) 

djserver: djmigrate
    $(DJANGO_BIN) runserver

djshell: djmigrate
    $(DJANGO_BIN) shell

djmigrate:
    if [ -z "$$($(DJANGO_BIN) migrate -l|grep '[ ]')" ]; then\

    else \
        $(DJANGO_BIN) migrate
    fi
